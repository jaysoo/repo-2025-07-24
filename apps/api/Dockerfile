# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @node-acme/api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t node-acme-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system node-acme-api && \
          adduser --system -G node-acme-api node-acme-api

COPY apps/api/dist node-acme-api/
COPY apps/api/package.json node-acme-api/
RUN chown -R node-acme-api:node-acme-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix node-acme-api --omit=dev -f install

CMD [ "node", "node-acme-api" ]
